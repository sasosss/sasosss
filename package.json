{
  "name": "persistent-civilization-simulator",
  "version": "1.0.0",
  "description": "Simulazione emergente di un mondo vivente in un singolo file HTML: civiltà, individui unici con memoria, guerre, religioni, economia e conversazioni con gli NPC.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node test/sim.test.js",
    "test:browser": "node test/browser.test.js",
    "test:longrun": "node test/longrun.js"
  },
  "devDependencies": {
    "puppeteer-core": "^25.3.0"
  }
}
